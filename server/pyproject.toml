[project]
name = "server"
version = "0.1.0"
description = "Production-ready FastAPI inference server for fine-tuned TinyLlama model"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "accelerate>=1.14.0",
    "fastapi>=0.138.1",
    "pydantic>=2.13.4",
    "torch>=2.12.1",
    "transformers>=5.12.1",
    "uvicorn[standard]>=0.49.0",
]
